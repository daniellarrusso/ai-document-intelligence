# Angular project rules

Use Angular 21 standalone components and the project's existing signals/reactive conventions. Prefer strict TypeScript, focused components, services for reusable logic, explicit loading/error/empty states, readable templates and focused Vitest tests. Avoid unnecessary subscriptions, `any`, and new state-management libraries unless justified.
