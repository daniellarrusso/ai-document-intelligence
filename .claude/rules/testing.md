# Testing rules

Test behaviour, not implementation details. For changed behaviour cover the happy path, meaningful edge cases and failure/security behaviour where relevant. Follow the repository's xUnit/Moq backend and Vitest/jsdom frontend conventions. Never weaken or delete tests just to make them pass.
